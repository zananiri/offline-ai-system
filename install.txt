Exact steps on the new computer
1. Copy the project folder (via USB, network share, git, whatever — minus .venv/)
2. Open PowerShell as Administrator in that folder, install system tools:
powershellSet-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
winget install --id Python.Python.3.11 -e --source winget
winget install --id Ollama.Ollama -e --source winget
winget install --id JohnMacFarlane.Pandoc -e --source winget
winget install --id UB-Mannheim.TesseractOCR -e --source winget
3. Create a fresh venv and install from the lock file (this is exactly why I had setup.ps1 generate requirements.lock.txt — it's your reproducible source of truth):
powershellpy -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.lock.txt
4. Add the __init__.py if it didn't survive the copy (check app/__init__.py exists; if not, create an empty one).
5. Start Ollama and pull the model (this downloads fresh on the new machine — the model isn't part of your project folder, it lives in Ollama's own storage):
powershellollama serve
New tab:
powershell.\.venv\Scripts\Activate.ps1
ollama pull gpt-oss:20b
ollama pull qwen2.5:32b
(qwen2.5:32b backs the Chat tab -- chat, document rewrite, and the two-step
LLM document-translation flow. See CHAT_MODEL / TRANSLATE_MODEL in app/ui.py.
This replaced the old dedicated Translate tab's offline MADLAD-400 pipeline,
which has been removed entirely -- translation is now just a chat request
like any other, not a separate model/tab.)
ollama pull hf.co/dicta-il/DictaLM-3.0-24B-Thinking-GGUF:Q4_K_M
(Legal tab model, ~14.3GB download -- must match LEGAL_MODEL in app/main.py
and app/ui.py. Skip/replace with the smaller 1.7B or IQ4_XS quant if this
machine is tight on RAM, but then update LEGAL_MODEL in both files too.)
ollama pull bge-m3
(Embedding model for the Attorney tabs' hybrid retrieval -- app/legal_pipeline_v2.py.
MUST match the model scripts/embed_local_law_pdfs_bgem3.py used to build the
'israeli_legal_db' collection.)
6. Build the Canon AI vector store (needs internet once, for chromadb + scraping):

If you copied app\chroma_db\ already → skip this step entirely.
If not:

powershellpip install chromadb
python scripts\scrape_cic_it.py
python scripts\embed_to_chroma.py
7. Run it, same as before:
powershelluvicorn app.main:app --host 0.0.0.0 --port 8000
new tab:
powershell.\.venv\Scripts\Activate.ps1
python -m app.ui
Steps 2–3 and 5–6 are the only genuinely new work — everything else is identical to first-time setup, just faster since you're not troubleshooting from scratch this time.